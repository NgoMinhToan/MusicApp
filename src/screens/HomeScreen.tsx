import React from 'react';
import {View} from 'react-native';
import Loading from '@screens/OnboardingScreen/Loading';

const HomeScreen = () => {
  return (
    <View>
      <Loading />
    </View>
  );
};

export default HomeScreen;
